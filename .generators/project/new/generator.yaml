sources:
  installFromRoot: true

transformations:
  replace:

    - name: kubernetesName
      token: acc-name
      with: "#kubernetesName"

    - name: gitUrl
      token: acc-git-url
      with: "#gitUrl"

    - name: gitBranch
      token: acc-git-branch
      with: "#gitBranch"
